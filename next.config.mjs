/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "videos.pexels.com",
      "firebasestorage.googleapis.com",
    ],
  },
};

export default nextConfig;
